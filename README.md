This script extracts human-readable settings from CoMPASS settings.xml files. It also returns a dataframe containing them, so it can be easily integrated into an analysis. 

Usage: 
python getCoMPASS.py /path/to/settings.xml
